Build with hugo.

```
$ hugo version
Hugo Static Site Generator v0.15 BuildDate: 2015-11-26T01:29:07-05:00
```

Develop with:

```
hugo server --buildDrafts --theme=mine --verbose
```

Generate site with:

```
hugo --theme=mine
```
